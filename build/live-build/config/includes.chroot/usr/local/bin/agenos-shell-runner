#!/bin/sh
set -eu

INSTALLER_APP="/usr/local/bin/agenos-installer-app"

launch_installer() {
  swaymsg workspace "1:home" >/dev/null 2>&1 || true
  "${INSTALLER_APP}"
}

while true; do
  launch_installer
  sleep 1
done
